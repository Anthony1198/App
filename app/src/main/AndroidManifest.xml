<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Körperfettanteil-Rechner"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListDataActivityFrau"></activity>
        <activity android:name=".LiniendiagrammFrau" />
        <activity android:name=".ListDataActivityMann" />
        <activity android:name=".FrauActivity" />
        <activity android:name=".MannActivity" />
        <activity android:name=".LiniendiagrammMann" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>